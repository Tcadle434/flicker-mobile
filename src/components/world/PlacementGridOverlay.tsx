/**
 * Placement Grid Overlay
 *
 * With pixel-level placement, the ghost item itself shows valid/invalid
 * via color tinting. This component is kept as a no-op for compatibility.
 */

export default function PlacementGridOverlay() {
  return null;
}
